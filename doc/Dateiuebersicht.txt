Datei übersicht:

// Roboter Arm: 
// Steuern der Roboter Arme, eine implementation für linker und rechter Arm.
// Beim task create kan über pvData die auswahl links und rechts gemacht werden.
// Zusatz task zum manuellen bedienen der Arme (vManualArmMovment())
// Die mittel Position ist mit einer mutex Semaphore geschüzt.
// Die Verschiedenen Arm Positionen sind in einer Tabelle abgespeichert und werden in einer vorschlaufe abgearbeitet.
// Zur syncronisation mit den Förderbänder werden weitere Semaphoren v
arm.c
arm.h

// Förderband:
// Steuert die Förderbänder und den verteil Arm. 
bcs.c
bcs.h

// Display and log:
// Display ansteuerung und log mesages für debug zwecke. Die Log Nachrichten werden am display
// angezeigt mit herkunft.
dispaly.c
dispaly.h

// CAN Kommunikation:
// Übernimmt die Kommunikation mit der Peripherie. Jedes Modul kann sich beim CAN
// anmelden und angeben welche ID an welche mesage queues gesendet werden.
// Um ein senden zu ermöglichen ist eine sende Funktion implementiert.
ucan.c
ucan.h


// 
main.c


